# 查看用例结果

- . 通过

- F 失败（用例执行是报错）

- E 出错（fixture执行报错）

- S 跳过

- X 预期外的通过（不符合预期）

- x 预期内的失败（符合预期）

# 用例发现规则

- venv/.开头的目录（发现不了）

- py文件，test*开头/test*结尾

- 类class不是用例，但会遍历所有Test开头的类

- 收集所有test\_开头的函数/方法

# Pytest对用例的要求：

- 可调用的（函数，方法，类）

- 名字 test\_ 开头

- 没有参数（参数有另外含义）

- 没有返回值 （默认none）r
